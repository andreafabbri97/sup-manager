// Auth component removed — the app no longer uses authentication.
// Left in repository as a placeholder to avoid breaking imports.

import React from 'react'

export default function Auth() {
  return null
}

